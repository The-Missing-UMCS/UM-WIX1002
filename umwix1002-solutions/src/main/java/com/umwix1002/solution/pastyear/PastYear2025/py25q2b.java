package com.umwix1002.solution.pastyear.PastYear2025;

public class py25q2b {
    // It will print out "Condition 3"
    // x (15) > y (5) is true, so it will go to the first if statement
    // y (5) > z (10) is false, so it will go to the else statement
    // z (10) > x (15) is false, so it will go to the else statement
}
